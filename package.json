{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=localhost",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "lint": "tsc --noEmit",
    "generate:rules": "node scripts/generate-firestore-rules.mjs",
    "deploy:hosting": "node scripts/firebase-deploy.mjs hosting",
    "deploy:rules": "node scripts/firebase-deploy.mjs rules",
    "deploy:prod": "npm run build && node scripts/firebase-deploy.mjs prod"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "firebase": "^12.11.0",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^6.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "tailwindcss": "^4.1.14",
    "terser": "^5.46.1",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
